---
import { COLLAB_STAGES } from "../../data/hiring";
import { url } from "../../lib/url";
---

<!-- ===== 7. COLLABORATIVE SEARCH ===== -->
<section class="sec" id="collaborative">
  <div class="wrap">
    <div class="sec-head rv">
      <p class="eyebrow is-brand">Collaborative Search<sup>®</sup></p>
      <h2>Hiring Shouldn't Happen in a Vacuum.</h2>
      <p class="lede">A new executive does not work with a job description. They work with people.</p>
      <p class="lede">HSG's Collaborative Search<sup>®</sup> approach looks beyond the hiring manager to better understand the people, relationships, expectations, and working environment surrounding the role. Depending on the engagement, HSG may help prepare interviews, participate in the interview process, conduct assessments, gather feedback, evaluate candidates, and support the client throughout the decision.</p>
    </div>

    <ol class="flow">
      {COLLAB_STAGES.map((st, i) => (
        <li class="step rv" style={`--d:${i + 1}`}><span class="n">{String(i + 1).padStart(2, "0")}</span><h4>{st.name}</h4></li>
      ))}
    </ol>

    <p class="flow-micro rv">Understand the company. Understand the candidate. Improve the decision.</p>

    <div class="flow-actions rv" style="display:flex; gap:12px; flex-wrap:wrap">
      <a class="btn" href="#close">Discuss Your Search <span class="arrow" aria-hidden="true">→</span></a>
      <a class="btn btn--ghost" href={url("/find-talent/#collaborative")}>Discover Collaborative Search<sup>®</sup></a>
    </div>
  </div>
</section>

