---
import { CONTACT } from "../../data/hiring";
import { SITE } from "../../data/site";
import CompanyIntake from "../CompanyIntake.astro";

/*
 * 13 Final CTA. "Start a Search" everywhere on this page lands here, on the
 * same employer intake the audience bar opens — one way to start a search,
 * not three. The `ft` prefix keeps its combobox ids apart from the bar's
 * copy, which is on this page too.
 */
---

<section class="sec ft-contact" id="start">
  <div class="wrap ft-contact-grid">
    <div class="ft-contact-copy rv">
      <p class="eyebrow is-hire">Start a Search</p>
      <h2>{CONTACT.title}</h2>
      <p class="lede">{CONTACT.copy}</p>

      <a class="ft-phone" href={`tel:${SITE.phoneHref}`}>
        <span class="ft-phone-ic" aria-hidden="true">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.9v3a2 2 0 0 1-2.2 2 19.8 19.8 0 0 1-8.6-3.1 19.5 19.5 0 0 1-6-6A19.8 19.8 0 0 1 2.1 4.2 2 2 0 0 1 4.1 2h3a2 2 0 0 1 2 1.7c.1.9.4 1.8.7 2.7a2 2 0 0 1-.5 2.1L8 9.8a16 16 0 0 0 6 6l1.3-1.3a2 2 0 0 1 2.1-.4c.9.3 1.8.6 2.7.7a2 2 0 0 1 1.7 2z"/></svg>
        </span>
        <span><small>Phone</small>{SITE.phone}</span>
      </a>
    </div>

    <div class="ft-intake rv" style="--d:1" data-aud-inline>
      <CompanyIntake idPrefix="ft" title="Start a Search" />
    </div>
  </div>
</section>

<script>
  import { initInlineIntakes } from "../../scripts/audience-menu";
  initInlineIntakes();
</script>
